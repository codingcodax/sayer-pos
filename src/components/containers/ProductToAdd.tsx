import { Box, Text } from '~/components/ui';

const ProductToAdd = () => {
  return (
    <Box css={{ bg: '$appBackground' }}>
      <Text>Product To Add component</Text>
    </Box>
  );
};

export default ProductToAdd;
